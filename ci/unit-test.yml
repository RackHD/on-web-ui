---
platform: linux

inputs:
- name:  ui
  path: "ui" 

run:
  path: bash
  args:
  - -exc
  -  |
     ./ci/check_focused_ut.sh
     apt-get update -y
     apt-get install -y chromium
     export CHROME_BIN=/usr/bin/chromium-browser
     npm install
     npm run unit-test
  dir: "ui"
